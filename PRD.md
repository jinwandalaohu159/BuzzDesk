# PRD

## 1. 产品目标

构建一个替代 Windows 原生桌面图标层的桌面 App。启动后隐藏原生桌面图标，只保留原本壁纸，并在壁纸上渲染新的桌面图标系统。

核心目标不是做一个普通启动器，而是做一个更丝滑、更适合收纳的桌面整理体验。重点围绕真实桌面图标、高质量图标显示、手机式虚拟文件夹、高级玻璃质感，以及 120fps 以上体感的拖拽与动画。

## 2. 桌面接管范围

App 启动后需要识别真正的 Windows 桌面，而不是当前窗口、当前屏幕截图或普通全屏界面。

原生桌面图标需要被隐藏，用户看到的是原壁纸加上我们重新渲染的图标层。

我们的桌面层应覆盖原生桌面图标，但不应永远盖住所有应用窗口。用户打开其他应用后，应用窗口应正常显示在桌面之上。

## 3. 图标来源与显示

需要扫描 Windows 桌面上的所有项目，包括快捷方式、应用、文件、文件夹，以及“回收站”“此电脑”等桌面虚拟图标。

图标必须使用软件或系统自带的真实图标，不能使用低清替代图、默认占位图或模糊图。

图标显示需要高清，支持在桌面图标和文件夹小图标之间缩放时仍保持清晰。

## 4. 桌面布局

第一版采用单屏横向网格布局。

排列方式从左到右，排满一行后换到下一行。不同于 Windows 原生桌面的纵向排列。

不做手机式多页桌面。收纳能力主要通过虚拟文件夹解决，而不是通过多页扩展空间。

布局需要自动适配屏幕尺寸，避免图标、文件夹、展开面板超出屏幕。

## 5. 普通图标操作

App 图标或桌面项目双击后打开对应真实应用、文件或系统项目。

图标可以被拖拽整理。

拖拽体验必须跟手，不能有明显延迟、卡顿或跳动。

## 6. 虚拟文件夹规则

文件夹是虚拟分组，不移动、不修改、不重排真实 Windows 桌面上的文件或快捷方式。

用户将一个图标拖到另一个图标上时，目标图标显示合并预览；用户松开鼠标后，才形成虚拟文件夹。

用户可以继续将图标拖入已有文件夹。

用户可以从文件夹中拖出图标。

文件夹中剩余 1 个图标时，文件夹自动解散。

## 7. 文件夹视觉

文件夹视觉逻辑参考手机桌面文件夹。

桌面上的文件夹表现为一个圆角矩形容器，内部显示缩小后的 App 图标。

文件夹封面内部不显示 App 名称。

文件夹容器比例可以自定义。

文件夹内部小图标尺寸可以统一调整。

文件夹图标下方显示文件夹名称。

## 8. 文件夹展开

点击文件夹后，文件夹居中展开为浮层面板。

展开面板需要根据屏幕尺寸自动控制大小，不能超出屏幕边界。

展开后，文件夹名称显示在顶部。

双击文件夹顶部名称可以进入重命名状态。

文件夹内的 App 图标双击后打开对应应用或项目。

## 9. 视觉设计

整体视觉需要有高级玻璃质感，但不能牺牲清晰度和性能。

文件夹封面、文件夹展开面板、选中态、拖拽悬停态可以使用半透明玻璃效果，表现为细腻的透明度、柔和高光、轻微描边、低噪点质感和适度阴影。

玻璃质感应服务于桌面壁纸融合感，不能让界面显得厚重、廉价、过度发光或影响图标识别。

文字和图标必须始终清晰可读。不同明暗壁纸下，文件夹名称、图标名称和面板标题都需要保持足够对比度。

视觉风格应干净、克制、精致，重点突出桌面图标和文件夹收纳，而不是做成复杂的装饰界面。

## 10. 动画要求

动画是核心体验，不是附加效果。

目标是 120fps 以上的体感，拖拽、合并、重排、展开都要尽可能顺滑。

拖拽时图标必须跟手。

图标重排不能瞬移，需要平滑过渡。

形成文件夹时，需要有连续的“靠近、吸附、缩小、收进圆角矩形”的动画。

点击文件夹展开时，应从文件夹位置自然过渡到居中面板，而不是直接弹窗。

动画需要可打断，例如拖到图标上后又移开，应自然回退；合并预览不能自动提交，只有松开鼠标时才提交。

## 11. 维护性要求

组件设计和交互设计必须分开。

视觉组件只负责展示，例如桌面图标、文件夹图标、文件夹面板、名称文本。

交互逻辑单独管理，例如拖拽、合并预览、松手提交、拖入文件夹、拖出文件夹、双击打开、重命名。

布局逻辑单独管理，例如横向网格、文件夹内部排列、屏幕边界适配。

目标是后期继续扩展时，不因为动画、组件、业务规则混在一起而难以维护。
